import 'package:flutter/material.dart';


class MencionesPage extends StatelessWidget {
  const MencionesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Menciones", style: TextStyle(color: Colors.white),),
    );
  }
}