import 'package:flutter/material.dart';


class VerificadoPage extends StatelessWidget {
  const VerificadoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Verificado", style: TextStyle(color: Colors.white),),
    );
  }
}