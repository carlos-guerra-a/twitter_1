import 'package:flutter/material.dart';


class TodasPage extends StatelessWidget {
  const TodasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Todas", style: TextStyle(color: Colors.white),),
    );
  }
}